/* d2k_quic.h — разбор QUIC Initial: узнать пакет и достать имя сайта.
 *
 * Браузеры всё чаще ходят по HTTP/3 поверх QUIC/UDP, а не по TLS поверх TCP.
 * Чтобы привязать обход к имени сайта, как это уже делает datapath/tls.c для
 * TCP, имя нужно достать из QUIC — но там оно не лежит открытым текстом: имя
 * (SNI) — часть ClientHello, а ClientHello — часть первого пакета соединения
 * (Initial), который зашифрован.
 *
 * ПОЧЕМУ ЭТО ВООБЩЕ МОЖНО СДЕЛАТЬ БЕЗ УЧАСТИЯ СТОРОН. Ключи Initial выводятся
 * не из секрета, а из идентификатора соединения (DCID), который сам лежит в
 * заголовке открытым текстом (RFC 9001 §5.2). Расшифровать клиентский Initial
 * может кто угодно на пути — просто взяв DCID из того же пакета и повторив
 * HKDF. Это не дыра в протоколе, а осознанное свойство: защита здесь — не от
 * подглядывания (от него защиты и не обещано), а от порчи неверсионными
 * посредниками (RFC 9001 §5.2: "This protection is not intended to provide
 * confidentiality... It is provided to protect QUIC packets from middleboxes
 * ... injecting or modifying packets"). Коробки провайдера именно этим и
 * пользуются, чтобы достать SNI и решить, резать соединение или нет; этот
 * модуль делает ту же арифметику, только ради противоположной цели.
 *
 * ДВЕ ВЕРСИИ, ДВЕ КОНСТАНТЫ. RFC 9369 (QUIC v2) намеренно меняет соль вывода
 * ключей, метки HKDF и даже нумерацию битов типа пакета — ровно затем, чтобы
 * коробка, зашитая на v1, спотыкалась. Обе функции здесь различают версию по
 * полю Version и не предполагают v1 умолчанием: захардкоженная на v1 сборка
 * не узнала бы v2-пакет вовсе, что и есть та самая ловушка (см. test_quic.c —
 * оба вектора, RFC 9001 A.2 и RFC 9369 A.2, проверены по отдельности).
 *
 * ЧЕГО ЭТОТ МОДУЛЬ НЕ ДЕЛАЕТ. Не пересобирает ClientHello, растянутый на
 * НЕСКОЛЬКО UDP-датаграмм: обе функции смотрят строго на буфер вызывающего,
 * без памяти о предыдущих вызовах и без выделений — расшифровка нужна
 * datapath'у на каждом UDP-пакете в горячем пути (следующая задача), а не раз
 * на соединение. Кадры CRYPTO ВНУТРИ одного пакета собираются по смещениям
 * (реальные браузеры режут ClientHello на несколько кадров в одном пакете —
 * см. quic.c), а вот кадр, растянутый на несколько пакетов, для одного вызова
 * попросту не виден: имя либо есть целиком в этом пакете, либо не найдено.
 * Второе — не ошибка модуля, а честный «в этом пакете имени нет».
 */
#ifndef D2K_QUIC_H
#define D2K_QUIC_H

#include <stddef.h>
#include <stdint.h>

#include "d2k_crypto.h" /* D2K_WARN_UNUSED и примитивы, на которых стоит расшифровка */

/* Узнаёт QUIC Initial ПО ЗАЩИЩЁННЫМ байтам — ключи не выводятся, крипто не
 * трогается вовсе. Работает потому, что защита заголовка (RFC 9001 §5.4.1)
 * закрывает только младшие 4 бита первого байта (зарезервированные биты и
 * длину номера пакета) и сам номер пакета — а два бита ТИПА длинного
 * заголовка (маска 0x30) остаются открытыми всегда, для ОБЕИХ версий.
 * Именно на этом стоит и датапат провайдера, и этот разбор.
 *
 * Возвращает 1, если p[0..n) — заголовок Initial версии 1 (RFC 9000) или
 * версии 2 (RFC 9369): длинный заголовок, тип "Initial" для СВОЕЙ версии
 * (типы перенумерованы между версиями — v1-Initial=0, v2-Initial=1, RFC 9369
 * §3.2), и все поля заголовка (DCID, SCID, токен, поле длины, да ещё и 16
 * байт на выборку для снятия защиты, RFC 9001 §5.4.2) целиком помещаются в n.
 *
 * Возвращает 0 на мусоре, на пакете другого типа (0-RTT/Handshake/Retry/
 * Version Negotiation) и на обрезанной датаграмме — все три варианта ЧЕСТНОЕ
 * «это не то, что мы умеем достать», а не ошибка использования: вызывающий
 * на горячем пути не обязан различать причину, только решить, пробовать ли
 * дальше d2k_quic_sni. */
int d2k_quic_is_initial(const uint8_t *p, size_t n);

/* Достаёт имя сайта (SNI) из ClientHello внутри QUIC Initial: снимает защиту
 * заголовка, выводит ключи ИЗ ЭТОГО ЖЕ DCID, расшифровывает, собирает кадры
 * CRYPTO по смещениям (они могут идти не по порядку передачи и с пропусками
 * — см. шапку quic.c) и разбирает ClientHello внутри получившегося потока.
 *
 * Возвращает 0 и заполняет out найденным именем (с завершающим нулём), ЕСЛИ
 * ВСЁ сразу: пакет — Initial, шифротекст проходит проверку тега AEAD ключами
 * из его же DCID, резервные биты заголовка после снятия обеих защит нулевые
 * (RFC 9000 §17.2 — иначе PROTOCOL_VIOLATION), кадры CRYPTO складываются в
 * ClientHello без разрыва хотя бы до конца расширения server_name, и это имя
 * целиком (вместе с завершающим нулём) помещается в cap байт out.
 *
 * Возвращает -1 во ВСЕХ остальных случаях — мусор, обрезанный вход,
 * неизвестная версия, чужой пакет, не раскрывшийся этими ключами (см.
 * предупреждение в шапке quic.c: пакет, ПОХОЖИЙ на нужный, но не
 * расшифровавшийся, — не более чем похож), ClientHello без имени (ECH,
 * возобновление сессии — законное состояние, не ошибка сети), разрыв в
 * кадрах CRYPTO раньше конца имени, имя не помещается в cap. out НЕ трогается
 * ни в одном из этих случаев — тот же контракт, что у d2k_aes128_gcm_decrypt
 * (d2k_crypto.h): молча принять нетронутый out за настоящее имя нельзя,
 * поэтому возврат отмечен D2K_WARN_UNUSED.
 *
 * Без выделений памяти: вся работа — на стеке фиксированного размера и по
 * буферу вызывающего, см. шапку файла и D2K_QUIC_MAX_DGRAM в quic.c. */
D2K_WARN_UNUSED
int d2k_quic_sni(const uint8_t *p, size_t n, char *out, size_t cap);

#endif
