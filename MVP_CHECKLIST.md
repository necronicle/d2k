# D2K — рабочий чеклист до MVP

Главное ТЗ: [D2K_SPEC.md](D2K_SPEC.md). База начала: `c595135`, 15.09.2026.
Идём по пунктам ниже. Галочка означает выполненную приёмку, не написанный
код, успешную компиляцию или один ответ собственного зонда.

## Актуальное продолжение: ревью и исправления 19.09

Исторические отметки ниже сохраняются, но не означают, что сквозная приёмка
пункта 5 пройдена. Подробности и границы текущих правок:
[передача — адресные опыты](docs/field/2026-09-19-address-trial-lifecycle.md).
Накопленное состояние сохранено коммитом `910d675`.
Продолжение: [UDP-доказательства и STUN](docs/field/2026-09-19-udp-evidence.md).
Текущая работа до MVP: [вход QUIC](docs/field/2026-09-19-quic-input.md).

- [x] Разделить подтверждения двух IP-целей с одинаковым plan_id: учитывать
  адрес и порт кандидата. Регрессия сначала падала, после правки проходит.
- [x] Не терять молчание текущего адресного опыта и события ухудшения при
  наблюдении: провести адресные задачи через общий жизненный цикл. Это ещё
  не законченная политика подтверждённой деградации/повторного поиска.
- [x] Снимать неподтверждённый адресный кандидат через DEL_ADDR, не DEL_NAME;
  истечение наблюдения не снимает подтверждённый план. Проверено по командам.
- [x] Починить Linux cross-рецепт test_quichello: новые зависимости присутствовали
  только в host-сборке. ARM64 musl и MIPS glibc теперь собираются и линкуются.
- [x] Запретить ложное подтверждение по обычному UDP EXCHANGE: это наблюдение,
  не новая коробка/привязка/успех; кандидат сохраняет ограниченный срок и очистку.
  Старые UDP CLIENT-привязки не восстанавливать автоматически, файл не стирать.
- [x] Исправить расхождение STUN с оригиналом: ошибка присутствующего
  XOR-MAPPED-ADDRESS возвращается оракулу; отсутствие атрибута по-прежнему допустимо.
- [ ] **Следующий блокер: подключить протокольное доказательство живого UDP.**
  Запрет ложного успеха — ещё не реализация правильного подтверждения.
  Связать ответ с реальным запросом/потоком/опытом, переиспользовать проверку
  оригинала; её вердикт хранить отдельно от успеха приложения. Сейчас голосовой
  и безымянный UDP-кандидаты НЕ получают постоянную привязку по клиентскому ответу.
- [x] Убрать потерю CRYPTO после восьмого кадра одного Initial: ограничивать
  разбор байтами, а не числом кадров. SNI и полный ClientHello проверены для
  QUIC v1/v2, прямого/обратного порядка и повреждённого тега.
- [ ] Собрать вход QUIC между датаграммами и передавать контекст без подмены
  формы. Проверить ещё одно расхождение: оригинал создаёт собственный Initial,
  а `fill_hellos` C-порта запрещает холодный старт без чужого снимка.
- [ ] Адресный QUIC-путь: один жёсткий delay=15000 — не завершённый перенос
  поиска и не переиспользование решений коробок. Проверить исходный QUIC-путь
  и фактический пробел при отсутствии имени, не придумывать новую лестницу.
- [ ] Проверить поколение опыта, точный контекст/порты, сохранение/восстановление
  адресного знания и переиспользование коробки; затем сквозной пункт 5.
- [ ] Голосовой runtime: сверить `voice_start` с переносом `voiceprobe`.
  Сейчас он ставит один фиксированный профиль на весь класс, обходя измеритель.
  Полевая непригодность STUN к отдельной сессионной точке не доказывает, что
  можно выкинуть исходный путь/лестницу для всех целей. Не считать карту CLI
  доказательством полного подключения оригинала к живому трафику.

## Основной чеклист

- [x] **1. Найденное измерителем воздействие точно исполняется Plan.**
  Закрыт ЛАБОРАТОРНО 17.09: оба стенда зелёные целиком, включая клиента за
  NAT. Поле — отдельный статус и остаётся непройденным.
  - [x] Сохранить конкретные байты приманки и контекст исходного измерения
    через возврат результата и копирование между потоками.
  - [x] Убрать пересборку приманки, замену длины на 64 и обрезание тела
    из пути передачи НАЙДЕННОГО воздействия.
  - [x] Сверить настоящим исполнителем длины/байты, seq, порядок, паузы,
    число повторов, порчу и судьбу оригинального пакета.
  - [x] Обработать MTU, сегментацию и несовместимый клиентский вход:
    не менять приём молча и не выдавать неполное исполнение за успех.
    Вход не той формы (старый TLS 1.2) НЕ подменяется похожим и назван вслух,
    но покрытие такого клиента принадлежит пункту 3, а не этому.
    - [x] Собирать полный наблюдаемый ClientHello из TCP-сегментов для снимка
      измерителя: ограниченная память, без удержания и изменения пакетов.
    - [x] Исполнять измеренное воздействие на составном клиентском входе:
      порядок целого входа, судьба всех оригиналов, таймаут и частичный сбой.
      Показано на стенде с цензором: обход найден за 63 с на пустом каталоге,
      исполнен на многосегментном приветствии, и НАСТОЯЩИЙ клиент получил
      ожидаемое содержимое. NAT сюда НЕ входит — см. следующий подпункт.
  - [x] Явно учесть пока невыразимые SYN-data, OOB и MD5; закрыть необходимые
    для выбранной полевой вертикали пробелы без подстановки «похожего».
    Переходник отказывается переводить такое плечо в Plan и называет причину
    поимённо (`detect/bridge.c`, проверено `test_bridge`), планировщик говорит
    это вслух и идёт к собранным кандидатам, а не выбрасывает цель. Приёма
    «похожего» нет нигде. Вертикали, выбранной для поля, эти три не нужны:
    она берётся `disorder`.
  - [x] Подтвердить одну сквозную вертикаль: измеритель нашёл → Plan исполнил
    то же воздействие → клиент за NAT получил доступ в том же протоколе.
    Стенд транзита 17.09 ЗЕЛЁНЫЙ: пустой каталог → цель под цензором → замер
    нашёл `disorder` за 61 с → план исполнен и подтверждён своим зондом →
    **клиент за NAT получил 200 три раза из трёх**, при живом цензоре, снявшем
    122 пакета по имени. Это ЛАБОРАТОРИЯ, не поле: цензор игрушечный
    (`iptables -m string` без сборки потока), протокол TLS 1.3, один класс
    коробки.
  - [x] **ПОЛЕ 18.09: та же вертикаль на живой линии.** Пустой каталог →
    discord.com под настоящей коробкой провайдера → замер нашёл «seqovl-1» за
    45 с и 15 зондов → план поставлен и ПОДТВЕРЖДЁН своим зондом (200) → curl
    с мака через роутер получил 200 четыре раза подряд, appconnect 0,13 с;
    у службы «подготовлен 5, ДОИСПОЛНЕН 5, выпущено 5, ошибок 0».
    Четыре причины, по которым этого не было раньше, разобраны замером в
    [отчёте](docs/field/2026-09-18-discord-vertical-works.md). Это ОДНА цель,
    TLS 1.3 и один класс коробки — область применимости остаётся пунктом 3.
- [x] **2. Убрать повторный старый поиск после оригинального.**
  Разделить полный результат перенесённого инструмента и старый результат
  «только класс». Сохранить исходные PREFIX/WHOLE и резервные ветки самого
  инструмента. Тест: завершённый поиск не запускает второй самодельный.
- [x] **3. Подтверждать исходным протоколом.** Закрыт 18.09, в поле.
  TLS 1.2 проверять TLS 1.2, TLS 1.3 — TLS 1.3; форма, полнота входа и
  область применимости явно учтены. Успех зонда не становится автоматически
  доказательством работы другого клиента или всего приложения.
  - [x] Зонд TLS 1.2 (`core/tls12.c`) и выбор протокола ПО ФОРМЕ КЛИЕНТА.
    Привязка пишется под ту форму, которой зонд действительно говорил —
    проверено тестом, который падает без правки. Живой обмен: самопроверка
    инструмента в стенде с цензором гоняет ОБА зонда и требует от каждого
    прикладного ответа и совпадения имени в сертификате.
  - [x] Вертикаль старого клиента целиком: стенд с цензором гоняет клиента,
    говорящего ТОЛЬКО по TLS 1.2 (`D2K_LAB_TLS12=1`). Поиск переигрывается
    его байтами, приём подтверждается зондом той же версии, привязка ложится
    под старую форму, и КРИТЕРИЙ 2 показан — клиент получил содержимое.
  - [x] Полнота входа. Подтверждение ведёт СОБСТВЕННОЕ рукопожатие зонда
    длиной клиента — чужой ClientHello ему не воспроизвести, так что байтами
    клиента приём добывается только на ЗАМЕРЕ. Привязка теперь помнит, чем
    мерили (`input`: байты клиента / заготовка холодного старта), и говорит
    это вслух. Заготовка перемеряется снимком клиента в любом порядке
    событий: снимок пришёл во время замера, во время испытания или после
    подтверждения (три теста `test_sched`, каждый был красным). Поле
    18.09, discord.com: замер начат заготовкой в 1534 байта, снимок клиента —
    321 байт одним сегментом; поиск повторён его байтами, тот же приём
    подтверждён, в каталоге роутера `"input": 1`, клиент получил 200. Цена —
    первый поиск на холодном старте вдвое дольше (90 с вместо 45).
  - [x] Область применимости. Другая цель — план узнанной коробки только
    кандидат, привязка лишь после своего зонда, промах привязки не заводит.
    Другая форма — ключ привязки и записи датапата «имя + форма», неизвестная
    форма не подходит ни к одной измеренной; две формы одной цели (браузер
    TLS 1.3 и старый клиент TLS 1.2) уезжают датапату каждая со своей —
    новый тест, проверен подменой. Другой протокол — привязки транспортов не
    перетирают друг друга, план чужого транспорта отвергается датапатом.
    Другой клиент — подтверждение зондом пишется третьим уровнем с
    источником PROBE; пассивный ответ и клиент другой формы его не поднимают.
- [x] **4. Довести оригинальные QUIC и voice/STUN-пути.** Закрыт 19.09
  решением Марка. Единственное непоказанное — пробой ГОЛОСА в поле, и он
  возможен только когда голос на линии режут; сегодня не режут.
  Карта переноса, тесты соответствия и подключение к трафику. Старый QUIC-код
  d2k не считать доказательством переноса оригинала. Адресным целям не
  придумывать домен.
  - [x] Карты переноса обоих путей: [QUIC](docs/field/2026-09-17-quic-port-map.md)
    и [голос/STUN](docs/field/2026-09-17-voice-port-map.md).
  - [x] QUIC: перенесены все семь вопросов вопросника (было — один), исходы
    записываются свойствами `d2k_quic_props`, а не текстом причины; приёмы,
    которые коробку ломают, а движку исполнить нечем, названы отдельными
    строками журнала. Вердикты `no_quic` и `local_address` заведены.
  - [x] Голос/STUN перенесён целиком: `core/stun.c` (векторы RFC 5769),
    `core/voice.c` (цель из живого разговора, три слоя, приёмы профиля),
    `core/d2kvoice.c`. Оракул замкнут кругом на настоящих сокетах.
  - [x] QUIC: выживание IP-фрагментов (`d2k_quic_arm.frag_survives`) меряется
    на заведомо отвечающем имени ДО боевого зонда — отрицательный ответ
    закрывает всё семейство ipfrag на линии, включая TCP-ветку; сырой входящий
    TTL (`d2k_quic_props.server_ttl_in`) снимается с первого подтверждённого
    ответа, без пересчёта в расстояние.
  - [x] Тесты соответствия для QUIC: `scripts/lab-quic-compare.sh` гоняет
    ЭТАЛОН и ПОРТ против одной и той же коробки, чьи способности известны по
    коду. Пять вопросов из семи совпали, один сверить нечем, один разошёлся —
    и разобран замером: у эталона между датаграммами одной попытки нет паузы,
    из-за чего его вопрос про мусор даёт ложное «не помогает».
    [Разбор](docs/field/2026-09-17-quic-compare.md).
  - [x] Тесты соответствия для голоса (19.09, `scripts/lab-voice-compare.sh`):
    общий вход у двух инструментов есть только у ПОИСКА ЦЕЛИ — таблица
    соединений, — его и сверяем. Эталон — сам исходный файл оригинала без
    правок (к нему приставлен только main), порт — `d2kvoice --list`; оба на
    роутере против одного и того же входа. Образец со всеми случаями
    расхождения: пять точек совпали числом пакетов, включая слияние двух
    клиентов одной точки; три расхождения объявлены с причинами — порт 50100
    (у оригинала тот же сдвиг, что был у d2k), IPv6 (датапат d2k только IPv4)
    и 100.64/10 (RFC 6598). Живой снимок таблицы роутера: две точки из двух.
    Первый прогон дал ложное расхождение — инструменты читали живую таблицу
    в разные моменты; теперь сверка идёт по одному снимку.
  - [x] Сверка голоса с боевым профилем z2k (18.09): пять изъянов — порты
    50100 вместо 50099, «молчат» без порога посланного, целью брался свой
    контроль, живость по точке вместо потока, запрещённый вывод в CLI — все
    исправлены тестами, проверенными подменой. [Разбор](docs/field/2026-09-17-voice-port-map.md).
  - [x] Подключение к трафику: датапат узнаёт IP Discovery (класс
    `@discord-voice`, не домен и не адрес — общий для всех точек, как
    `nohost` у z2k); план — своя приманка Initial QUIC ×10 перед ним;
    контроллер испытывает приём на самом разговоре и подтверждает ответом
    сервера по потоку клиента. В поле 19.09 узнавание и событие обмена
    показаны на настоящем звонке; испытание приёма — нет: голос на линии
    сегодня не режется.
  - [x] Поле QUIC (19.09): discord.com по QUIC на линии режется (000 без d2k),
    через d2k проходит. Найдена причина, по которой датапат не узнавал цель:
    настоящий клиент разбрасывает ClientHello по множеству мелких кадров
    CRYPTO в разнобой поперёк ДВУХ Initial-датаграмм — то же слепое пятно, что
    у GFW (USENIX Security 2025). Обход сделан ЯВНЫМ: примитив `delay`
    (REC_DELAY, minexec=6) разносит датаграммы во времени; план применён 6 раз
    на 6 попыток, все 200. [Разбор](docs/field/2026-09-19-quic-field.md).
  - [~] Поле голоса: показано наблюдение на живом звонке (узнаны все шесть
    потоков, ответ сервера дал событие обмена), но НЕ пробой — блокировки
    голоса на линии не было. Закрыто заочно решением Марка 19.09: ждать
    блокировку, чтобы показать пробой, значит остановить работу.
- [ ] **5. Пройти сквозную приёмку MVP.**
  Пустой каталог → живой трафик → решение → работа приложения → проверка
  собственного решения на второй совместимой цели → повторный поиск после
  подтверждённой деградации. Работающий прямой трафик не запускает подбор.
  Чистые контрольные измерения, изоляция проб, перезапуск и сохранение знания.

Полный продуктовый перенос на C, включая оставшуюся серверную Go-панель,
остаётся требованием D2K_SPEC, а не незаметно вычеркнутой частью работ.

## Текущее продолжение внутри пункта 1

Первые два подпункта закрыты локальными тестами в коммите, содержащем
[отчёт measured → Plan](docs/field/2026-09-15-measured-plan.md).
Сегментация, интервалы и вместимость результата исправлены; сессия собирает
все 22 пакета длинного тестового варианта. Но весь пункт остаётся открытым.

Заголовки сверены двумя настоящими кодировщиками на 32 сочетаниях;
максимальная приманка помещается в текстовый слот. Пункт 2 закрыт регрессиями
четырёх исходов оригинального инструмента, его внутренний поиск сохранён.
Подробности и красные стенды: [передача 16.09](docs/field/2026-09-16-wire-and-search.md).

**Продолжение 17.09.** Незакоммиченная работа по удержанию оригиналов
зафиксирована и закрыла разрыв исполнения: стенд с цензором зелёный целиком,
включая настоящего клиента. Перенесена отмена замера, которой не было вовсе:
без неё истёкший срок ОДНОЙ задачи блокировал главный поток контроллера на всё
время её замера, и служба не выходила по сигналу. В журнал добавлены
длительность и цена поиска, потолок задачи и оговорка про непокрытого старого
клиента. Разбор: [передача 17.09](docs/field/2026-09-17-hold-and-cancel.md).

**Пункт 1 закрыт лабораторно 17.09.** Обе половины разрыва сошлись: датапат
удерживает оригиналы до сборки приветствия и исполняет план на составном
входе, а вертикаль доведена до клиента ЗА NAT. Оба стенда зелёные целиком.

По дороге выяснилось, что стенды с ВНЕШНЕЙ целью мерили чужую линию: трафик
уходит с машины, а на роутере автора поднят z2k, чьё правило ловит весь 443 по
порту. Один и тот же код на местной цели находит приём за 55 с и 18 зондов, на
внешней — 294 зонда за 16,8 минуты и ничего. Цель стендов переехала в своё
сетевое пространство; внешняя осталась ключом и читается с оговоркой.

**Разобрано 17.09: промах conntrack был гонкой чтения.** Запись существует,
первый запрос её не видит, повторный сразу же — видит. Цена промаха
несоразмерна редкости: попав на поток собственного зонда, он губил ВЕСЬ
найденный обход по цели («прошло 0 из 30» на рабочем плане). Промах теперь
перепроверяется тремя попытками; настоящее отсутствие записи по-прежнему
отвергает план. Разбор, числа и то, что осталось незакрытым:
[промах conntrack](docs/field/2026-09-17-conntrack-race.md).

**Пункт 4 начат и наполовину закрыт 17.09.** Вопросник QUIC доведён с одного
вопроса до семи, голосовой путь перенесён с нуля. Обе половины — лабораторно:
поля не было ни у одной. Ограничения перечислены подпунктами выше и подробно —
в двух картах переноса.

Историческое следующее действие на 17.09 — пункт 3; актуальное — в начале файла.

**Пункт 3 начат 17.09.** Зонд TLS 1.2 перенесён и работает: планировщик
выбирает протокол подтверждения по форме клиента, привязка пишется под ту
форму, которой зонд говорил. Осталось в пункте: полнота входа (зонд шлёт своё
приветствие, а не байты клиента) и явная область применимости. Дальше — пункт
4 (QUIC и voice/STUN) и пункт 5 (сквозная приёмка).

Не начинать новую архитектуру поиска и не ставить галочку за полевой опыт
по успешной лаборатории.

## Правила фиксации результата

Для закрытия подпункта записать commit, тест/опыт и ограничения. Поле и
локальная проверка — отдельные статусы. Не ставить галочку всему пункту 1
по одному исправленному полю. До изменения роутера проверить его фактическое
состояние, чистоту измерения и возможность отката.

Предыдущие исправления адаптера/потоков и их проверки:
[отчёт 15.09](docs/field/2026-09-15-adapter-and-concurrency.md).
